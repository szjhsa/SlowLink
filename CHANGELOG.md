# 更新日志

本项目遵循语义化版本号。发布更新日志由本文件对应版本章节自动生成。

## [1.39.10] - 2026-08-09

- 网页新增“去重统计”：按规则、来源、抽奖身份统计命中和去重次数，方便定位是哪些正则或来源经常被去重。
- 全局抽奖身份拦截时新增碰撞记录：显示被拦消息、来源、链接和命中身份，网页可直接“标记为不同抽奖”。
- 标记后该条文本在 7 天内不再被同一个抽奖身份拦截，避免误挡后只能手动解除一次。
- 记录中心新增“去重统计”页签，并提供清空碰撞记录；监听、转发、去重策略和 watchdog 行为不变。

## [1.39.9] - 2026-08-09

- 抽奖去重新增通用全局身份：标题 + 截止/开奖时间 + 奖品即可关联，作为第二把去重钥匙与严格模板身份同时生效。
- 口令、活动详情、发布群组顺序不再拆散同一场抽奖；截止时间、开奖时间或奖品变化仍视为不同抽奖。
- 抽奖 ID / 随机种子仍然最优先，`global / id / off` 三种抽奖模板模式继续生效，其余监听、转发和 watchdog 行为不变。

## [1.39.8] - 2026-08-09

- 修复同一场“抽奖活动已开始”模板分别在不同群发布时，仅因发布群组顺序不同而重复转发的问题。
- 当模板缺少口令或活动详情、但有标题、截止时间和奖品时，也会生成 10 分钟全局抽奖身份；发布群组顺序不影响身份，截止时间或奖品变化仍视为不同抽奖。
- 完整模板、抽奖 ID、随机种子、刮刮乐、注册码、监听、转发和 watchdog 行为保持不变。

## [1.39.7] - 2026-08-08

- 消息队列忙时自动增加普通处理线程，空闲约 45 秒后自动降回低水位，1 核服务器平时更省 CPU，高峰时不会因为队列积压拖慢转发。
- 事件、命中、失败、性能记录改为批量写入 Redis 并增加每日统计；网页新增 `/api/health_summary`，可查看当天转发、去重、失败和系统事件汇总。
- 监听启动优先加载保存在 Redis 中的实体索引，避免每次重启都全量 `get_dialogs`，会话较多时启动更快、Telegram API 压力更小；手动刷新或测试发送时仍会完整重建。
- 抽奖去重新增全局跨模板模式：`全局` 对所有抽奖模板统一去重（默认），`仅 ID / 种子` 只对明确抽奖标识去重，`关闭` 只按原文本去重；不再单独绑定刮刮乐或代金券。
- 正则匹配、完整码识别、去重策略、监听、重连、watchdog 和网页其余功能保持不变。

## [1.39.6] - 2026-08-08

- 将“刮刮乐”跨模板去重从 `元代金券` 专用判断改为全局 `scratch-event` 身份，代金券、注册码、白名单码及其他奖品类型统一走同一套奖品区提取逻辑。
- 统一解析 `奖品` / `奖品内容` 的单奖品和多奖品列表，保留数量、区分不同奖品列表；金额奖品会去掉模板中的品牌前缀，注册码等文字奖品保留完整内容，并且不再按来源频道限制关联。
- 补充精简模板奖品区结束边界，避免把“现在可以参与抽奖”“祝你好运”等说明文字算进奖品；用户正则、消息触发、监听、转发和其他去重逻辑保持不变。

## [1.39.5] - 2026-08-08

- 修复同一场“刮刮乐 + 单个代金券”抽奖分别使用完整模板、精简模板和带品牌奖品名模板发布时，因文本指纹不同而重复转发的问题。
- 仅在消息明确属于抽奖、包含“刮刮乐”且奖品区只有一个“金额 + 元代金券”时生成 10 分钟跨模板身份；品牌前缀会被忽略，金额和数量仍参与身份计算，因此 `10元代金券` 与 `20元代金券` 不会互相误挡。
- 是否转发仍完全由用户配置的正则决定；普通聊天、多奖品刮刮乐、原有按来源区分的币奖品去重，以及监听、转发和其他业务逻辑保持不变。

## [1.39.4] - 2026-08-03

- 网页 `/invite/` 注册码辅助提取由固定六位扩展为固定六位或八位字母数字，支持 `https://ue2.taotu.ink/invite/5fbbddb9` 等新面板链接；五位、七位、九位及含符号尾码仍不会被误识别。
- 是否转发仍完全由用户在后台配置的正则决定；辅助提取只服务于码身份和去重，不会把 `/invite/` 链接变成系统内置触发条件。
- 八位尾码现与六位尾码使用同一套跨域名去重：相同尾码即使域名不同也视为同一个注册码，不同尾码保持独立；现有监听、正则、转发及其他去重行为不变。

## [1.39.3] - 2026-08-03

- 新增任意 HTTP/HTTPS 域名下 `/invite/六位字母数字` 注册码的辅助提取，例如 `https://ep.whooping.top/invite/486549` 会提取尾码 `486549`。
- 该格式不会成为系统内置触发条件；只有用户在后台配置的正则命中时才会转发，未命中正则仍保持不转发。
- 文本去重保留六位尾码指纹：不同尾码不会再因 URL 统一归一化而互相误挡，相同尾码即使来自不同域名仍按同一个注册码去重。
- 保持现有用户正则原文匹配、Register/Renew、Whitelist、Telegram start 深链、监听、转发、HTTPS 和 watchdog 行为不变。

## [1.39.2] - 2026-08-02

- 一键安装新增两种网页访问方式：推荐的域名 HTTPS，以及兼容原有行为的 IP + 自定义端口 HTTP；安装后可通过 `manage.sh web` 随时切换。
- 新增可选 `slowlink_caddy` 容器，HTTPS 模式只由 Caddy 暴露 80/443，`slowlink_app` 的管理端口改为仅绑定 `127.0.0.1`；HTTP 模式继续支持自定义端口。
- 更新会保留 `.env` 中的访问模式、域名和端口，只重建 `slowlink_app`；域名未变化且 Caddy 正常运行时不会重启 Caddy，也不会停止 Redis、Assistant Bot 或其他服务。
- 网页模式切换和版本更新均增加完整回滚，失败时恢复原模式、绑定地址、端口与域名；HTTPS 健康检查会验证真实证书和 `/health`。
- 保留数据卸载会移除应用与 Caddy 容器，但保留配置、Telegram Session、Redis 数据和 Caddy 证书卷；`PURGE` 二次确认后才删除 SlowLink 自有数据卷。
- 将 Telethon 固定为已验证的 `1.44.0`，避免后续构建自动漂移到未经验证的版本；消息监听、识别、正则、去重和转发业务逻辑保持不变。

## [1.39.1] - 2026-07-31

- 支持 Telegram 新版 `RichMessage` 抽奖/投票消息：当普通正文为空时，从内容区块中还原可匹配文本并保留区块换行，`ycdgroup/72` 这类消息不再因正文为空而漏检。
- 普通 Telegram 消息仍优先使用原始正文；富消息按钮文字、按钮链接和视频内容不会加入匹配文本，避免扩大关键词误命中范围。
- 优先队列与实际匹配统一使用同一正文提取逻辑，含“抽奖”等内容的富消息继续走优先处理通道。
- 将已知旧规则 `\n\n🎁 抽奖活动已开始` 精确迁移为 `(?m)^抽奖活动已开始！?$`；其它自定义正则保持原样。

## [1.39] - 2026-07-31

- `Register/Renew` 猜码尾段恢复中文提示支持，`CineTrail-30-Register_l83UX删除4vlP2` 等原文现在可直接识别、测试命中并转发；系统不会删除提示词或改写转发内容。
- 中文猜码与原有 `@`、`.`、`#`、`%`、星号遮挡及 `数字/字母` 占位共用同一提取、触发和去重规则，不再只针对“删除”维护硬编码词表。
- `Whitelist` 支持夹入中文删除提示，但仍要求尾段包含 10 个实际英文数字字符，并继续遵守“先命中正则才转发”的保护规则，避免放宽为任意长度白名单码。
- 自动迁移 Redis 中 V1.38.100 的已知 Register/Renew 与 Whitelist 系统规则；其它用户正则保持原文、原含义和 V1.38.100 的匹配方式不变。
- 保留注册码使用通知拦截、句末说明边界、原始大小写、不同猜码独立去重以及现有监听、转发和 CPU watchdog 行为。

## [1.38.100] - 2026-07-29

- 用户配置的每条“正则规则”现在都会按原文编译，并直接匹配 Telegram 原始消息；系统不再先将消息做 NFKC 转换后执行用户正则。
- 移除“无正则元字就降级为关键词”和“普通多行内容自动拆成多条”的隐式改写；仅保留网页明确约定的 `;;` 批量分隔符。
- 全角与半角标点、括号、换行和正则元字是否匹配完全由用户规则决定；网页测试器与实际监听使用同一逻辑。
- 安全过滤、排除文本、码识别、消息分类和去重继续使用规范化文本；现有 `re.I | re.M`、8KB 长度保护和编译缓存保持不变。

## [1.38.99] - 2026-07-29

- 修复同一场抽奖分别以“参与关键词”群消息和“点击按钮参与”频道消息发布时，因创建者显示和正文不同而被重复转发的问题。
- 在标题、开奖时间、奖品、参与要求和活动说明全部完整时生成 10 分钟跨模板关联身份；忽略创建者显示、参与方式、实时人数和中奖概率。
- 任一稳定字段变化、字段不完整或超过 10 分钟时均不使用该关联拦截；普通文本、邀请码、720 分钟抽奖去重及 Telegram 监听转发逻辑保持不变。

## [1.38.98] - 2026-07-22

- 新增 Telegram Bot `start` 深链识别，支持 `t.me` 和 `telegram.me` 中缺少项目名前缀的 `30-Register_随机码`、`30-Renew_随机码`，这类完整链接现在可以直接触发转发。
- 识别范围限定为 Bot 用户名和结构化 `start` 参数；普通文本里的裸 `30-Register_随机码` 仍不触发，避免放宽全局正则造成误转发。
- 文本去重会保留批量深链中的每个 `start` 码指纹，不同批次不会因链接统一归一化而被错误视为相同文本。

## [1.38.97] - 2026-07-21

- 修复更新时依赖文件内容已变化、但文件大小和归档时间戳相同导致 Docker BuildKit 继续使用旧依赖快照的问题。
- 更新前先替换全部程序文件，并对 `slowlink_app` 执行无缓存构建，确保 `requirements.txt` 中的依赖版本真实进入运行容器。
- 保留 `.env`、Telegram Session、Redis 数据、备份和用户配置；仍只重建 `slowlink_app`，不重启 Redis、Assistant Bot 或其他服务。
- 保持 Telegram 监听、消息识别、正则、去重、转发和 Web 业务逻辑不变。

## [1.38.96] - 2026-07-21

- 将 Waitress 从 `3.0.0` 升级到 `3.0.2`，修复上游已确认的半开 socket 竞态：旧版可能让 Web 主线程在 `select()` 中持续忙循环并占满单核。
- 保持 Telegram 监听、消息识别、正则、去重、转发、Redis 和 Web 轮询业务逻辑不变；现有 CPU watchdog 继续作为异常情况下的自动恢复兜底。
- 增加依赖版本回归测试，防止后续发布重新引入存在该忙循环问题的 Waitress `3.0.0`。

## [1.38.95] - 2026-07-19

- `Register/Renew` 完整码后缀新增可见特殊字符支持，包括 `@`、`.`、`#`、`%`、`+`、`=`、`&`、括号、斜杠和星形符号等猜码字符；例如 `帝服-30-Register_mK@nxdnuwU` 现在可以完整识别并直接触发。
- 统一更新完整码触发、Markdown 提取、使用通知保护、文本测试和完整码去重指纹，特殊字符不会在不同处理层被截断或丢失。
- 保留中文正文边界：码后接空白、逗号、句号或说明文字时只提取完整码；未知中文片段仍不会被误吞进随机码。
- 自动迁移 Redis 中上一版的纯码正则和码识别规则，无需手动删除或重新录入现有规则；旧字母数字、`数字/字母` 占位和星号遮挡格式保持兼容。

## [1.38.94] - 2026-07-18

- 根据两次线上满核记录将问题范围确认到主程序监听线程：正常阶段平均 CPU 约 1%–2%，异常阶段会突然连续约一分钟占满单核；本版不通过降低阈值掩盖问题。
- 自动迁移已知的旧版“自由 / 定时 / 开放注册”嵌套正则，改为逐行线性匹配；保留原有公告命中语义，不修改用户其他正则、码识别、去重和转发逻辑。
- CPU watchdog 优先读取 cgroup v2 的 `usage_usec`，按相邻检查窗口计算真实容器 CPU；旧环境仍回退到 Docker 统计，原有 `90% × 4`、十分钟冷却和只重启 `slowlink_app` 的保护策略保持不变。
- 首次 CPU 超限时连续采集三份 Python 全线程堆栈，并记录同一时间窗口内各线程的 CPU 增量、消息流和最终容器快照，便于下一次直接区分 Telethon 更新处理与消息匹配热点。
- 堆栈信号改为容器内发送 `SIGUSR1`，不再通过 `docker kill` 产生多余的 Docker restart-manager 日志；诊断内容仍只写入独立 `watchdog.log`。

## [1.38.93] - 2026-07-17

- Web 后台新增“排除文本”管理：消息包含任意一条已配置关键词时直接排除，即使同时命中正则或码识别也不转发。
- 排除关键词使用 NFKC 标准化并对英文大小写不敏感，保留原始展示内容；匹配结果缓存 60 秒，Web 增删或导入后会立即失效缓存。
- 正则测试器新增“命中排除文本”原因和具体关键词；排除文本同步纳入配置导入、导出和启动自检。
- 默认排除文本列表为空；保持现有监听来源排除、正则、码识别、去重、监听、转发和日志行为不变。

## [1.38.92] - 2026-07-17

- 修复管理员状态面板只因出现“定时注册”就触发注册公告规则的边界问题；现在优先读取独立的“注册状态”字段。
- `注册状态 | True/On/Enabled/1/开启` 仅放行到原正则继续判断，不强制转发；`注册状态 | False/Off/Disabled/0/关闭` 在原正则前拦截。
- `定时注册 | Turn off` 只表示定时功能关闭，不再当作当前注册关闭；因此“注册状态=True + 定时注册=Turn off”仍会按原规则转发。
- 保持无“注册状态”字段的自由/定时注册公告、注册码、抽奖、去重、监听和转发逻辑不变。

## [1.38.91] - 2026-07-16

- 修复同一抽奖先发布精简正文、随后补充“参与要求”并再次发布时因文本指纹不同而重复转发的问题；实际案例 `Lewa_movie/390202` 与 `Lewa_movie/390205` 现在会关联为同一场抽奖。
- 为“截止时间 + 奖品 + 发布群组 + 口令 + 活动详情”模板增加十分钟稳定关联身份，忽略容易后补或编辑的参与要求段。
- 标题、截止时间、奖品、发布群组、口令或活动详情任一稳定字段变化时仍视为新抽奖；超过十分钟后不再使用该短期关联身份拦截。
- 保持普通文本去重、抽奖 ID、随机种子、刮刮乐、注册码、监听、转发和重连逻辑不变。

## [1.38.90] - 2026-07-16

- 修正手动刷新群/频道列表时成功日志输出过早的问题：刷新结果通过完整性保护并写入缓存后才记录成功，不再出现“已刷新”后紧接“结果疑似不完整”的矛盾提示。
- 疑似不完整的刷新仍会保留旧列表并写入网页失败记录，但 Docker 日志只输出一条警告，不再为同一结果重复打印 `ERROR` 和 `WARNING`。
- 保持监听、识别、去重、转发、Telegram 高延迟重连和 CPU watchdog 行为不变。

## [1.38.89] - 2026-07-16

- 修正 V1.38.88 的 Whitelist 触发顺序：Whitelist 格式本身不再绕过主规则直接触发，必须先命中用户配置的正则或关键词。
- 主规则命中后继续提取固定十位 Whitelist 完整码，保留 `strong_whitelist` 身份用于完整码去重；相同码拦截，不同后缀放行。
- Whitelist 仍进入优先队列；九位、十一位、非 ASCII 字母数字、使用记录和关闭/无效上下文仍不会作为有效完整码转发。
- 网页说明同步改为“正则命中 -> 完整码提取 -> 去重 -> 转发”，不修改既有 Register/Renew 行为。

## [1.38.88] - 2026-07-16

- 新增固定十位纯字母数字的 `项目名[-更多项目段]-Whitelist_随机码` 内置强格式，例如 `WindMoon-Whitelist_1OTb0O0FMO`；无需主正则即可完整识别并直接触发转发。
- Whitelist 随机码保留原始大小写并生成独立完整码身份，相同码按“完整码重复”设置拦截，不同后缀互不影响。
- Whitelist 消息进入优先队列；九位、十一位、含下划线或缺少项目名前缀的近似文本不会作为完整码触发，使用记录和关闭/无效上下文继续由底层安全过滤拦截。
- 网页码识别区域补充 Whitelist 强格式说明；不修改现有 Register/Renew、正则、普通去重和转发逻辑。

## [1.38.87] - 2026-07-15

- 网页宿主机端口支持自定义：Docker Compose 使用 `SLOWLINK_WEB_PORT`，一键安装支持菜单输入和 `--port` 参数，端口会写入 `.env` 并在后续更新中保留；安装前会检查端口占用、显示占用详情并推荐空闲端口，不会停止现有服务。
- 修复一键安装在已有服务占用 `8080` 时构建后才失败的问题；基础依赖齐全时跳过 APT，Redis 通过健康检查后才构建应用，并将镜像构建与容器启动拆分为独立中文诊断步骤。
- 更新过程新增程序文件事务备份与失败回滚：复制新版、保存端口、构建、启动及完整验证任一步失败都会尝试恢复上一版；回滚本身失败时保留旧程序备份并显示路径，已有实例不能误走无备份安装；`.env`、Telegram Session、Redis 数据和用户配置不进入程序覆盖范围，首次安装失败只清理 `slowlink_app`。
- 安装完成前校验容器版本、Redis、网页 `/health` 和 CPU watchdog；`manage.sh status` 增加当前网页端口与访问地址，Docker Compose 等待 Redis 健康且仍只重建 `slowlink_app`。
- 运行诊断写入 Redis 改为尽力而为，Redis 短暂写入失败不再中断消息处理；消息 worker 增加状态统计、异常退出检测和自动补建，失败队列 worker 也纳入活跃任务计数。
- 轻量重连增加 worker 暂停门闩：等待当前正在处理的普通、优先及失败重试任务结束后再断开，新任务会留到重连完成后处理，避免转发发送途中被重连打断；移除两小时重连时多余的过期去重 key 扫描，不修改原有 FloodWait 重试语义。
- `/health` 同时检查 Redis、监听期望状态、监听线程、Telegram 连接、五分钟内心跳和 worker 完整性；期望运行但尚未真正就绪，或期望停止但监听仍存活时返回降级状态，便于 Docker 和安装器发现假健康。
- 缓存显示时区并支持保存后立即失效，未命中消息延后执行时间格式化；匹配器复用已规范化文本生成紧凑文本，减少高消息量下的重复处理，识别、正则、去重和转发业务规则保持不变。

## [1.38.86] - 2026-07-15

- 精简 Telegram 高延迟轻量重连日志：一次重连只记录准备重连、转发结果和重连结果，不再重复打印开始、完成或失败状态。
- 重连原因不再附带原始正则片段，仅保留延迟、来源和连续次数等必要诊断信息。
- 高延迟重连事件统一通过网页事件通道写入，网页日志与 Docker 日志使用同一条文案。
- 连续 30 分钟未收到消息时改用简短中文提示，并以最后一条消息或最后一次重连为计时基准，避免安静频道反复重连；高延迟阈值、缓存复用和转发业务逻辑保持不变。

## [1.38.85] - 2026-07-15

- 网页后台“账号与转发 → 转发目标”新增公开链接格式切换，可选 `t.me 标准模式` 或 `telegram.me 兼容模式`。
- 新安装或尚未保存该设置时默认使用 `t.me`；非法或未知配置也会安全回退到 `t.me`。
- 保存后立即清理监听线程的设置缓存，后续新转发无需重启监听即可使用新域名。
- 私有群及私有话题链接始终保留 `https://t.me/c/...`，不受公开链接模式影响。
- 公开链接格式纳入配置备份与恢复，容器重建和服务器重启不会丢失用户选择。

## [1.38.84] - 2026-07-14

- 公开群和频道的帖子链接改用 `https://telegram.me/用户名/消息ID`，绕过当前 `t.me` 返回空网页预览的缓存路径。
- 公开论坛话题链接同步使用 `telegram.me`，保留话题 ID 和消息 ID 的原有结构。
- 私有群链接继续使用 `https://t.me/c/...`，不改变私有消息跳转兼容性。
- 识别、去重、监听和转发流程保持不变，本版本只调整公开消息链接的域名。

## [1.38.83] - 2026-07-14

- 修复同一场跨群抽奖在“无随机种子”和“带随机种子”两种模板中分别走全文与稳定 ID 去重、导致重复转发的问题。
- 高置信普通抽奖会根据创建者、参与关键词、开奖时间和规范化奖品清单生成 10 分钟跨模板关联指纹。
- 带抽奖 ID、随机种子和无稳定 ID 的模板都会检查同一关联指纹，并支持不同来源群的同一抽奖相互去重。
- 开奖时间或奖品清单发生变化时不会共享指纹；两个明确且不同的随机种子仍优先视为不同抽奖。
- 奖品指纹保留小数点，避免把 `1.5` 与 `15` 这类不同奖品数值误判为相同。

## [1.38.82] - 2026-07-14

- 修复“自由注册成功”个人账号通知被开放注册正则误判并转发的问题。
- 在所有关键词、正则和完整码触发前增加窄范围安全过滤：只有独立的“注册成功”状态同时带有创建账号、账号有效期或到期时间字段时才拦截。
- “自由注册已开启”等真实开放注册公告继续正常命中；“自由注册成功率提升”等包含相似字样的公告不会被误过滤。
- 网页正则测试会明确显示“个人注册成功通知过滤”，便于区分规则未命中与底层安全拦截。

## [1.38.81] - 2026-07-14

- 修复同一场无抽奖 ID 的刮刮乐使用“抽奖活动已开始”和“抽奖开始啦”两种模板时被重复转发的问题。
- 对能够明确提取来源、刮刮乐模式和至少两个币值奖品的消息，增加 10 分钟短时模板关联去重。
- 不同来源、不同奖品清单或超过 10 分钟后发布的新抽奖仍会正常转发，避免把后续正常活动误判为重复。
- 原有抽奖 ID、随机种子、规范化全文、邀请码和原消息链接去重逻辑保持不变。

## [1.38.80] - 2026-07-14

- 修复同一个抽奖以“参与关键词”和“参与条件”两种模板发布时被重复转发的问题。
- 抽奖消息现在优先提取明确的“抽奖 ID”或“随机种子哈希”作为稳定去重身份，不再把这些唯一字段丢弃后只比较正文。
- 相同稳定 ID 在抽奖去重时间内会记录为“相同抽奖 ID 重复”；不同 ID 即使创建者和奖品相同也会正常转发。
- 没有稳定 ID 的抽奖继续使用原有规范化文本去重，不启用容易误挡不同活动的模糊相似判断。

## [1.38.79] - 2026-07-12

- 支持强格式 `Register/Renew` 码后缀中用于遮挡的字面量 `*`，只在完整码结构内清理，不全局删除符号。
- `GuaiCum-30-Register_ZlB5*cqm*u` 会被完整提取为 `GuaiCum-30-Register_ZlB5cqmu`，避免截成半个码参与去重。
- 完整 `Register/Renew` 强格式码可以独立触发转发，不再依赖额外的主正则或“已为您生成”上下文。
- 继续过滤“注册码使用”等使用通知，也不会把含其它未知标点的后缀擅自拼接成邀请码。

## [1.38.78] - 2026-07-12

- 支持 `Register/Renew` 随机后缀中的字面量 `数字`、`字母` 遮罩标记，避免完整码被截断或无法触发。
- 多行遮罩注册码会为每一行生成独立文本去重指纹，不再因漏提取而错误合并。
- 自动迁移 Redis 中旧的 ASCII 纯码触发规则和码识别规则，无需手工修改正则。
- 未知中文后缀不会被截成半个邀请码参与去重，继续避免宽泛中文误识别。

## [1.38.77] - 2026-07-11

- 发布安全测试改为检查敏感运行文件未被 Git 跟踪且被 `.gitignore` 忽略，不再要求删除本地 `.env`、Session、运行数据或 watchdog 日志。
- 补充 `secrets/`、Token、私钥、证书和 `credentials.json` 的忽略规则。
- 发布回归测试改为从 `VERSION` 读取当前版本，避免正常发版时重复修改历史测试文件。
- 整理历史 GitHub Release：移除线上更新日志附件，使用完整中文更新正文，并让 SHA-256 只校验两个 ZIP 资产。

## [1.38.76] - 2026-07-11

- 将 `SIGUSR1` Python 全线程调用栈从 Docker 标准日志隔离到容器内临时诊断文件，再由 watchdog 写入宿主机 `watchdog.log`。
- 每轮高 CPU 只在首次超限时采集一次调用栈，持续到自动重启时不再重复打印。
- watchdog 不再复制应用容器最近 160 行日志，避免调用栈在诊断日志中累积重复。
- 保留持续高 CPU 自动重启、Redis 状态、Telegram Session 和监听自动恢复行为。

## [1.38.75] - 2026-07-11

- 修复 Release 解压后源码时间固定且文件大小相同时，Docker BuildKit 可能复用旧构建上下文，导致宿主已更新但容器仍运行旧版本的问题。
- 部署前主动刷新 app 源码文件时间；健康检查后强制核对容器内版本与 `VERSION`。
- 版本不一致时自动执行一次 `slowlink_app` 无缓存重建并再次校验，整个过程仍不停止 Redis 或其他服务。

## [1.38.74] - 2026-07-11

- 监听入口增加动态 chat ID 快速过滤，未进入监听列表的消息在进入业务处理前直接跳过，降低大账号消息风暴时的单核 CPU 压力。
- 手工填写且暂时无法解析的监听项自动退回原有过滤路径，避免优化导致漏消息；监听配置修改后无需重启即可刷新过滤集合。
- 增加一分钟消息流诊断，记录收到、快速过滤、入队、命中、转发和队列数量，便于定位 Telegram 补发或群消息洪峰。
- 应用启用 SIGUSR1 Python 全线程调用栈输出；watchdog 在首次高 CPU 和持续高 CPU 时保存调用栈、消息流和线程快照。
- watchdog 不再记录主机进程完整命令参数，避免诊断日志意外包含其他服务的 Token 或密码。
- 保留原有持续高 CPU 自动重启、Redis 数据、Session 和监听自动恢复行为；不修改匹配、去重或转发业务逻辑。

## [1.38.73] - 2026-07-11

- 初始化公开 Git 仓库和 `main` 分支，清理仓库根目录历史版本包与运行时文件。
- 增加 GitHub Actions 标签发布流程，自动测试并生成 app 包、full 包、更新日志和 `SHA256SUMS.txt`。
- 增加中文一键安装、更新、卸载和管理脚本，支持 Ubuntu、Debian 与 `curl | sudo bash`。
- 更新时保留 `.env`、`data`、Telegram Session、Redis 数据和用户配置，只重建 `slowlink_app`。
- 保留现有 CPU watchdog 和 Redis 监听状态自动恢复逻辑。
- 本版本不修改消息监听、识别、去重或转发业务逻辑。
